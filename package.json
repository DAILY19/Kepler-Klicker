{
  "name": "castastrophe",
  "version": "1.0.0",
  "private": true,
  "description": "Castastrophe! - Browser-based multiplayer fishing party game",
  "scripts": {
    "start": "powershell -ExecutionPolicy Bypass -File start.ps1",
    "serve": "npx http-server . -p 8080 -c-1 --cors",
    "deploy:rules": "firebase deploy --only database"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
